# Start here: the law, in Aden's words

Student zero · node `or.start` · 2026-08-29

## The law

A node on this board lights when it has a URL: a link to work a stranger can open and check. Effort does not light anything. Hours do not light anything. A checkbox does not light anything. The unit of progress is something someone else can inspect.

That rule exists because real teams work the same way. Your report that you tried is not evidence. The artifact is.

## Parallel pair this week

Two nodes are open with no line between them, so they can run in the same week:

1. **It runs (`pf.runs`)**: clone the studio repo, boot Northline Desk, paste the status output that shows T-1842 on Support. That is the next spine step after this note.
2. **The four skills (`cv.four`)**: write CCVV in my own words with one honest example each. Independent of the boot; I can draft it while a long install runs, or after.

How I split the week: boot first until the status command is green and the gist exists, then the four-skills page before Monday. World and graph stay off until those two are lit. Depth waits.

## Done when (this node)

This page is the evidence URL. The law is above, in my words, with no appeal to effort or hours. The parallel pair is named.
